#include <stdio.h>
/**
 * main - writes name of prog
 *argc - argument count
 *argv - array of argc pointers
 *return: 0
**/
int main(int argc, char *argv[])
{
	printf("my name is %s\n", argv[0]);
	return (0);
}
